# ecommerce_test

## Running the test

- Run the command:

```bash
cp .env.example .env
```

- Then update the `.env` with secrets from you Cloud Grid

- Start the test by running:

```bash
yarn test
```
